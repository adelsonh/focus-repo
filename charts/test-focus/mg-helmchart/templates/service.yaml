apiVersion: v1
kind: Service
metadata:
  name: {{ include "mg-helmchart.fullname" . }}
  labels:
    {{- include "mg-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mg-helmchart.selectorLabels" . | nindent 4 }}
