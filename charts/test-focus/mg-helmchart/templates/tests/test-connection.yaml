apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mg-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "mg-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mg-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
